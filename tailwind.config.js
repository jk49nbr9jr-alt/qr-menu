export default {
  // Enable class-based dark mode; we toggle the `dark` class on <html>
  darkMode: 'class',
  content: ["./index.html", "./src/**/*.{ts,tsx,js,jsx}"],
  theme: {
    extend: {
      // place custom tokens here if needed later
    },
  },
  plugins: [],
}